////////////////////////////////////////////////////////////////////////////
//*Number the divs of the document , up to the div #last//
////////////////////////////////////////////////////////////////////////////

$('div').each(function(){
	// Invoke for each div 
	// Create a jQuery object from the element
	// And insert the index at start of it.
	$(this).prepend(idx + ": ");
	// Stop iterating when we reach the #last
	if(this.id === "last" )
		return false;
});




////////////////////////////////////////////////////////////////////////////
//* End of snippet *//
////////////////////////////////////////////////////////////////////////////
